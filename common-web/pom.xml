<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.example</groupId>
        <artifactId>microservices-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>common-web</artifactId>
    <version>1.0.0</version>
    <name>common-web</name>
    <description>Web library for exception handling and web utilities</description>

    <dependencies>
        <dependency>
            <groupId>com.example</groupId>
            <artifactId>common-core</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <!--
             GlobalExceptionHandler rethrows AccessDeniedException/AuthenticationException so
             Spring Security's own filter chain can translate them to 403/401. "provided"
             because every service that pulls in common-web already has spring-security-core
             at runtime (transitively via spring-boot-starter-security or
             spring-boot-starter-oauth2-resource-server), so we only need it here at
             compile/test time, not as an extra runtime dependency of common-web itself.
        -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-core</artifactId>
            <scope>provided</scope>
        </dependency>
        <!--
             GlobalExceptionHandler maps org.springframework.dao.DataIntegrityViolationException
             (defined in spring-tx) to 409. Compile scope on purpose: Spring resolves the classes
             named in @ExceptionHandler when the advice bean starts, so every consumer of common-web
             must have spring-tx on its classpath, including services without a persistence layer.
        -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
        </dependency>

        <!-- Distributed Tracing. Boot 4 moved the Micrometer Tracing / Zipkin auto-configuration into
             dedicated modules; this starter brings them plus the Brave bridge and the Zipkin reporter.
             The bare micrometer-tracing-bridge-brave + zipkin-reporter-brave pair (Boot 3 style) no
             longer activates tracing on its own. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-zipkin</artifactId>
        </dependency>

        <!-- Structured Logging -->
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.loki4j</groupId>
            <artifactId>loki-logback-appender</artifactId>
        </dependency>

        <!-- Prometheus Metrics -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
